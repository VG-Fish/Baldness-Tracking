# To use this app:
1. Clone this repo using `git clone https://github.com/VG-Fish/Baldness-Tracking.git`
2. Install all the necessary Python libraries stated in pyproject.toml.
3. Run `streamlit run app.py`
