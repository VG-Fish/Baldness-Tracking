# To use this app:
- Clone this repo using `git clone https://github.com/VG-Fish/Baldness-Tracking.git`
- Install all the necessary Python libraries stated in pyproject.toml using `uv pip install -r pyproject.toml`
  - This command assumes that you already have `uv` installed.
- Run `streamlit run app.py`
